<resources>

    <style name="Theme.Flow" parent="Theme.Flow.DesignSystem"/>

    <style name="Theme.Flow.Leanback"/>

    <style name="Theme.Flow.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.Flow</item>
    </style>
</resources>
